import { GameContext, GameProvider } from "./game";
import { HighestScoreContext, HighestScoreProvider } from "./highest-score";
import { ThemeContext, ThemeProvider } from "./theme";

export {
    GameContext,
    GameProvider,
    HighestScoreContext,
    HighestScoreProvider,
    ThemeContext,
    ThemeProvider,
};
